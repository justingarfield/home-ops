---
apiVersion: notification.toolkit.fluxcd.io/v1beta1
kind: Provider
metadata:
  name: public-discord
  namespace: flux-system
spec:
  type: discord
  channel: staging
  suspend: true
  secretRef:
    name: public-discord-webhook-url
