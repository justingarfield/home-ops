---
apiVersion: notification.toolkit.fluxcd.io/v1beta2
kind: Provider
metadata:
  name: public-discord
  namespace: flux-system
spec:
  type: discord
  channel: staging
  suspend: false # don't use this, it will hold-up any "dependsOn" resources for other Kustomizations
  secretRef:
    name: public-discord-webhook-url
