---
apiVersion: notification.toolkit.fluxcd.io/v1beta2
kind: Provider
metadata:
  name: public-discord
spec:
  type: discord
  channel: staging
  suspend: true
  secretRef:
    name: public-discord-webhook-url
