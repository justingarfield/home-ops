---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Propagates to sub-folders and child resources
namespace: flux-system

resources:
  - git
  - helm
