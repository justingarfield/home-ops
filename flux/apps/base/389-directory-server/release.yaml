apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: 389-directory-server
  namespace: 389-directory-server
spec:
  releaseName: 389-directory-server
  chart:
    spec:
      chart: 389server
      version: "2.2"
      sourceRef:
        kind: HelmRepository
        name: johanneskastl-389server
        namespace: flux-system
  interval: 5m
  install:
    remediation:
      retries: 3
