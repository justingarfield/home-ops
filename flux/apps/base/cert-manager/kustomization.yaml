---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - repository.yaml
  # renovate: registryUrl=https://charts.jetstack.io chart=cert-manager
  - https://github.com/cert-manager/cert-manager/releases/download/v1.11.0/cert-manager.crds.yaml
  - release.yaml
  - cluster-issuers
secretGenerator:
  - name: cert-manager-secrets
    files:
      - cert-manager-secrets.sops.yaml
configurations:
  - kustomizeconfig.yaml
