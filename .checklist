task generate-pki
task talos-prepare-configs

task vbox:create-cluster
task vbox:start-cluster

task talos-prepare-nodes

task talos-bootstrap-nodes
task talos-install-cilium

kubectl get nodes

task flux:create-namespace
task flux:create-sops-age-secret
task flux:bootstrap-flux-cd GITHUB_TOKEN=<token>

flux logs
flux get all
