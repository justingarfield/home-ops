---
# yaml-language-server: $schema=https://talos.dev/v1.4/schemas/v1alpha1_config.schema.json

# This patch file:
#   * Adds additional certificate SANs that represent my
#     HA Proxy endpoint used for communications via talosctl.
#
# Note: I also add my HA Proxy Static IP address to the list
#       below, later-on when calling "talosctl gen config ...".

machine:
  certSANs:
    - talos-endpoints.staging.home.arpa
    - talos-endpoints
